1. yarn create react-app my-app --template typescript = create proj with typescript
2. yarn start = to run this proj
3. yarn add axios = install axios
4. yarn add @types/react-router-dom --dev = install route
